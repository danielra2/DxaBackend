package mycode.dxa.enrollment.models;

public enum EnrollmentStatus {
    ACTIVE,
    DROPPED,
    COMPLETED

    //folositor mai tarziu, daca un student se inscrie la un curs si renuna / vrea refund etc
}